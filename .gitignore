# Secrets & local config
.env
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.mypy_cache/
.ruff_cache/
.pytest_cache/
# Hypothesis's example database and its downloaded unicode tables. A sweep
# committed `.hypothesis/unicode_data/…/charmap.json.gz` once because this line
# was missing; it is a regenerable cache, never source.
.hypothesis/
.coverage
.coverage.*
htmlcov/

# Data & runtime artifacts
data/
artifacts/
.local/
models/
mlruns/
reports/
*.parquet
*.csv
!tests/fixtures/*.csv
logs/
*.log

# IB Gateway / IBC local state
ibc/
tws_settings/

# Agent worktrees. `claude --worktree` and `remote-control --spawn worktree` cut a
# nested checkout here; git does not ignore a linked worktree on its parent's behalf,
# so without this the parent reads as dirty for as long as the worktree lives.
.claude/worktrees/

# OS / editor
.DS_Store
Thumbs.db
